#!/usr/bin/python

from math import sqrt

a = 4
b = 0
c = -4

delta = b**2 - 4*a*c

if delta>=0:
    x = {'1' : (-b + sqrt(delta))/(2*a),
         '2' : (-b - sqrt(delta))/(2*a)}
    print x['1'], x['2']
else:
    print "Pas de solution"
